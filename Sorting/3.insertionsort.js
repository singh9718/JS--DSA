// -- O(n2) in worst case
// -- In-place and stable
// -- Used in practice for small arrays (TimSort and IntroSort)
// -- O(n) in best case
// -- O(n) in Best Case
// -- best case happens when array is already sorted and worst case happens when array is reverse sorted.
// -- time complexity in best case is θ(n) -- already sorted
// worst case: reverse sorted θ(n2)
// in general : O(n2)